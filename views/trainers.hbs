<!DOCTYPE html>
<html lang="uk">

<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="d-flex flex-column min-vh-100">
{{> menu}}
  <div class="container mt-5">
    <h1 class="text-center mb-5 fw-bold text-uppercase">Наша Команда</h1>

    <div class="row">
      {{#each trainersList}}
      <div class="col-lg-6 mb-4">
        <div class="card shadow h-100 border-0 overflow-hidden">
          <div class="row g-0 h-100">
            <div class="col-md-5 bg-light d-flex align-items-center justify-content-center overflow-hidden">
              {{#if this.photo}}
              <img src="/trainer/image/{{this.id}}" class="img-fluid h-100 w-100 object-fit-cover"
                style="min-height: 250px;" alt="{{this.full_name}}">
              {{else}}
              <div class="text-muted p-5">Фото відсутнє</div>
              {{/if}}
            </div>

            <div class="col-md-7">
              <div class="card-body d-flex flex-column h-100">
                <h4 class="card-title fw-bold text-accent">{{this.full_name}}</h4>
                <span class="badge bg-secondary mb-2 w-50">Досвід: {{this.experience}} років</span>
                <p class="card-text flex-grow-1">{{this.description}}</p>
                <hr>
                <p class="mb-1"><small>📧 {{this.email}}</small></p>
                <p class="mb-0"><small>📞 {{this.phone}}</small></p>
              </div>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
{{> footer}}
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>